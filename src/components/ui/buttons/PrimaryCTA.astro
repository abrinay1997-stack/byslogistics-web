---
import Icon from '@components/ui/icons/Icon.astro';
// Destructure the properties from Astro.props
const { title, url, noArrow, size = 'md' } = Astro.props;
// Define TypeScript interface for the properties
interface Props {
  title?: string;
  url?: string;
  noArrow?: boolean;
  /** `lg` para los llamados principales (hero y cierres de página). */
  size?: 'md' | 'lg';
}
// Define CSS classes for styling the button
const baseClasses =
  'group inline-flex items-center justify-center gap-x-2 rounded-full font-bold text-neutral-50 ring-zinc-500 transition duration-300 focus-visible:ring-3 outline-hidden';
const sizeClasses =
  size === 'lg'
    ? 'px-8 py-4 text-base sm:text-lg'
    : 'px-6 py-3.5 text-sm 2xl:text-base';
const borderClasses = 'border border-transparent';
// Relieve y desplazamiento al pasar el cursor: el botón principal tiene que
// pesar más que el resto de la página, no confundirse con un enlace.
const bgColorClasses =
  'bg-brand-600 shadow-lg shadow-brand-600/25 hover:bg-brand-700 hover:shadow-xl hover:shadow-brand-700/30 hover:-translate-y-0.5 active:translate-y-0 active:bg-brand-700 dark:focus:outline-hidden';
const disableClasses = 'disabled:pointer-events-none disabled:opacity-50';
const ringClasses = 'dark:ring-zinc-200';
---

{/* Link styled as a button, with dynamic title, URL, and optional arrow */}
<a
  class={`${baseClasses} ${sizeClasses} ${borderClasses} ${bgColorClasses} ${disableClasses} ${ringClasses}`}
  href={url}
>
  {title}
  {/* Display the arrow based on the 'noArrow' property */}

  {
    noArrow ? null : (
      <span class="transition-transform duration-300 group-hover:translate-x-1">
        <Icon name="arrowRight" />
      </span>
    )
  }
</a>
